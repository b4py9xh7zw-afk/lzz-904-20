# FFmpeg Filter Lab 自测说明

本说明根据项目交付标准（result.md）整理，用于验证项目的交付质量与工程完备性。

## 1. 硬性门槛验证
- [x] **运行环境完备性**：项目支持通过 `docker compose up --build` 一键启动，无需在宿主机安装 PHP 或 FFmpeg 环境。
- [x] **功能可验证性**：浏览器访问 `http://localhost:8080` 后，所有核心功能（上传、调参、预览、保存）均可真实操作并得到预期反馈。
- [x] **主题一致性**：交付产物完全聚焦于“FFmpeg 风格滤镜调试工具”这一业务主题。

## 2. 交付完整性验证
- [x] **核心功能覆盖**：已实现视频/图片输入、滑块实时调参、FFmpeg 命令动态生成、处理后效果预览、以及预设保存与加载功能。
- [x] **全链路闭环**：提供了从素材上传到结果下载的完整业务流，而非仅展示 UI 或部分代码片段。
- [x] **文档随附**：包含 `README.md` 操作指南、`presets.json` 示例数据以及完整的 Docker 环境配置。

## 3. 工程与架构质量验证
- [x] **合理的工程结构**：采用了前后端分离（通过 API 通讯）的清晰布局，前端逻辑集中在 `index.php`，后端处理逻辑集中在 `process.php`。
- [x] **环境隔离与持久化**：通过使用 Docker 命名卷（Named Volumes）无缝兼容 Windows/Mac 平台，实现了预览文件的持久化存储，无需在宿主机创建特定系统路径。
- [x] **可维护性设计**：滤镜生成逻辑采用参数化拼接，易于未来新增其他 FFmpeg 滤镜项（如更多 eq 选项）。

## 4. 工程细节与专业度验证
- [x] **健壮的错误处理**：后端对文件上传、FFmpeg 执行返回码、输出文件生成状态进行了严格校验，并能将具体错误捕获后反馈至前端控制台。
- [x] **输入校验与安全**：对所有滑块/数字输入进行了严格的类型转换与范围限定，使用 `escapeshellarg` 规避命令注入风险。
- [x] **性能与资源控制**：引入 `timeout 15` 限制处理时长，并对视频预览进行了时长截取（5s），确保本地调试的即时性。
- [x] **状态反馈机制**：前端包含加载遮罩（Loading Overlay）、操作提示（Toast）以及控制台日志输出，符合生产级产品的交互逻辑。

## 5. 需求理解与适配度验证
- [x] **业务场景匹配**：精准响应了“快速试错”这一需求，提供了“重置”和“快速预设切换”功能，优化了调参循环。
- [x] **隐含约束达成**：严格遵守了不依赖 Node.js、不使用数据库、限制文件大小与处理时长等技术约束。

## 6. 美观度与交互体验验证
- [x] **视觉美感**：界面采用深色系玻璃拟态（Glassmorphism）设计，配色方案统一且具有现代科技感。
- [x] **交互流畅性**：摒弃了原生 `alert/prompt` 弹窗，所有交互（如保存预设）均通过自定义模态框和 Toast 指令完成。
- [x] **响应式布局**：页面在不同尺寸屏幕下均能保持合理的比例分配，对比区域在宽屏下支持左右分栏。
